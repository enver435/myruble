import {
    connect
} from 'react-redux';
import {
    bindActionCreators
} from 'redux';

// import screen
import Withdraw from '../../screens/Tabs/Withdraw';

export default connect(null, null)(Withdraw);