import {
    connect
} from 'react-redux';
import {
    bindActionCreators
} from 'redux';

// import screen
import MyReferrals from '../screens/MyReferrals';

export default connect(null, null)(MyReferrals);